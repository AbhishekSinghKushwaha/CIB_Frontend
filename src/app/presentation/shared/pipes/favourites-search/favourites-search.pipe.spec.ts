import { FavouritesSearchPipe } from './favourites-search.pipe';

describe('FavouriteSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FavouritesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
