import { PaymentCategorySearchPipe } from './payment-category-search.pipe';

describe('PaymentCategorySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentCategorySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
