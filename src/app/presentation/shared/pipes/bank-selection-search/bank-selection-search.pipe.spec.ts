import { BankSelectionSearchPipe } from './bank-selection-search.pipe';

describe('BankSelectionSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BankSelectionSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
